```
Create a single self-contained HTML file with an interactive learning artifact for the concept: "[CONCEPT]".

Context: [1-2 sentences about your current level and what you're trying to learn]

Requirements:
- Single HTML file, no external dependencies except CDNs (Chart.js, etc. from cdnjs.cloudflare.com)
- Interactive: sliders, inputs, buttons, live state — not just static content
- The artifact should do one of: simulate, quiz, visualize, or let me build/manipulate the concept hands-on
- Clean, minimal UI — flat design, no gradients or shadows, white/gray surfaces
- Dark mode safe — use CSS variables for all colors, never hardcode hex for text/backgrounds
- Mobile friendly

Artifact type (pick the best fit for the concept):
- Flashcard/quiz app
- Interactive diagram with clickable nodes
- Simulator / scenario walkthrough
- Decision tree / flowchart navigator
- Before/after code comparison tool
- Checklist / tracker with progress

Specific behavior I want:
[Describe exactly what it should do — e.g. "show me a DDD aggregate with clickable child entities that explain their role"]

Output: A single .html file I can open in a browser or embed.
```